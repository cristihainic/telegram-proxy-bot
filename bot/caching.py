CACHE = {
    'synced': False,
    'ban_list': [],
    'reply_targets': {},  # {operator_id: (target_id, target_name, expires_at)}
    'name_cache': {},     # {user_id: user_info dict from Telegram}
}
